<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.myConsole.modules.oss.dao.SysOssDao">

    <select id="findByIds" parameterType="java.lang.String" resultType="com.myConsole.modules.oss.entity.SysOssEntity">
        select * from sys_oss where id in (#{ids})
    </select>
</mapper>